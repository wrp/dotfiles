
Clean up the commit message in ga.  When multiple files
are committed, should summarize the directory in the
first line instead of just "Add files".  Maybe just
mention the directory with the most commits, or find the common
directory of all files added, or ???
