# vi .bashrc .bash_profile .vimrc .inputrc .tmux.conf
#
# .bashrc
test -f /etc/bashrc && . /etc/bashrc
unalias -a
trap 'history -a' DEBUG
EVENT_NOKQUEUE=1
HISTTIMEFORMAT='%H:%M:%S '
HISTFILESIZE=9999999999
LSCOLORS=fxfxcxdxbxegedabagacad
LESS=-FeRX
LC_TIME=C  #  Get 24 hour times for %X (sar)
set -o vi
PS1='\[$( { test $? != 0 && tput setaf 1 || tput setaf 2;} 2> /dev/null
	)\][$(date "+%H:%M:%S") $$ \u@\h]\[$( tput setaf 2 2> /dev/null )\]\$ '
PROMPT_COMMAND='printf "\033k%s\033\\" "${HOSTNAME##*-}"'

# .bash_profile
[[ "$-" =~ i ]] && test -f $HOME/.bashrc && . $HOME/.bashrc


" .vimrc
nore v V
nore V v
nore ; :
nore , ;
set nowrapscan
let loaded_matchparen = 1

# .inputrc
set editing-mode vi
set keymap vi

# .tmux.conf
unbind C-b
set -g prefix C-k
bind C-k send-prefix
set -g window-status-current-bg yellow
set -g window-status-current-fg red
set -g window-status-current-format '#I:#W'
set -g status-left-length 25
set status-right '%H:%M %e-%b'
set-option -g update-environment "SSH_AUTH_SOCK"
set history-limit 25000
bind-key -n Left previous-window
bind-key -n Right next-window
bind-key , command-prompt "rename-window"
bind-key '$' command-prompt "rename-session"
