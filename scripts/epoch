#!/usr/bin/env perl

use strict;
use warnings;
use Getopt::Std;

my %args;
getopts('t', \%args);


while(<>) {
	if ($args{t}) {
		s/([0-9]{9,10})/sprintf "%s (%s local)", $1, scalar localtime $1/ge;
	} else {
		s/([0-9]{9,10})/sprintf "%s (%s GMT)", $1, scalar gmtime $1/ge;
	}
	print;
}
