#!/bin/sh

if test -n "$*"; then
	for x; do echo "$x"; done
else
	cat;
fi \
| perl -pe 's/([0-9]{9,10})/sprintf "%s (%s)", $1, scalar localtime $1/ge'

#| perl -pe 'ntf "%10s: %s\n", $_, scalar localtime $_'
