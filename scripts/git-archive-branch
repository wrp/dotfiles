#!/bin/sh

for branch; do
	git update-ref refs/archive/"${branch}" "${branch}" '' \
	&& git branch -D "${branch}"
done

if test $# = 0 && ! git show-ref \
	| grep refs/archive --color=never
then
	base=$(git rev-parse --absolute-git-dir)
	echo "There are no archived refs in $base" >&2
	exit 1
fi
