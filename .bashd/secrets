
for _file in $HOME/.config/$USER/secrets/*.gpg; do
	_name=$(basename "${_file%.gpg}")
	if ! _value=$(gpg -qd "$_file"); then
		echo "gpg failure.  Unable to assign $_name" >&2
	else
		eval "export '$_name'='$_value'"
	fi
done
unset _file _name _value
